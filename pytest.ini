[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: requires Redis + PostgreSQL running
    slow: long-running tests (model inference, load tests)
addopts = -v --tb=short -m "not integration and not slow"
